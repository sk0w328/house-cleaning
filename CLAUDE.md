# CLAUDE.md

## プロジェクト情報

- **プロジェクト名**: cleaning-app
- **アプリ名**: そろそろお掃除アプリ
- **概要**: 忘れがちな掃除場所を登録し、最後に掃除した日からの経過日数を表示。掃除頻度に応じて状態を自動判定するアプリ
- **返答は必ず日本語でお願いします**

---

## 技術スタック

- HTML
- CSS
- JavaScript（バニラJS／外部ライブラリなし）

---

## ディレクトリ構成

```
cleaning-app/
├── CLAUDE.md
├── index.html
├── style.css
└── app.js
```

---

## 機能要件

### 基本機能
- 掃除場所の**新規登録**（場所名・推奨掃除頻度を設定）
- 掃除場所の**削除**
- 「今日掃除した」ボタンで**最終掃除日を更新**
- 最終掃除日からの**経過日数を表示**
- 掃除頻度に応じた**状態の自動判定**（下記ロジック参照）
- データは **localStorage** に保存（リロードしても消えない）

### 対象外（デモ版のため除外）
- プッシュ通知
- リマインダー機能

---

## 状態判定ロジック

経過日数と推奨頻度（日）を比較して3段階で自動判定する。

| 状態 | 条件 | 表示例 |
|------|------|--------|
| ✅ きれい | 経過日数 ≤ 推奨頻度 × 0.8 | 緑色で表示 |
| ⚠️ そろそろ | 推奨頻度 × 0.8 < 経過日数 ≤ 推奨頻度 × 1.2 | 黄色で表示 |
| 🚨 要掃除 | 経過日数 > 推奨頻度 × 1.2 | 赤色で表示 |

---

## データ構造（localStorage）

キー名: `cleaning-app-data`

```json
[
  {
    "id": "一意のID（Date.now()などで生成）",
    "place": "場所名（例: トイレ、キッチン）",
    "frequency": 7,
    "lastCleaned": "2024-01-15"
  }
]
```

---

## UI要件

- **モバイルファースト**のレスポンシブデザイン
- カード形式で各掃除場所を表示
- 状態に応じた色分け（緑 / 黄 / 赤）
- シンプルで直感的な操作感

---

## コーディング規約

- コードのコメントは**日本語**で記載
- 初心者でも読みやすいシンプルなコードを心がける
- 関数は役割ごとに分割して記載する

---

## Git運用ルール

### リポジトリ情報

- **GitHubリポジトリ**: https://github.com/sk0w328/house-cleaning.git
- **メインブランチ**: `main`

### 基本ルール

1. **コードを変更するたびに必ずGitHubにプッシュする**
2. コミットメッセージは**日本語**で、変更内容が伝わるように書く
3. 1つのコミットには1つの変更をまとめる（複数の無関係な変更を混ぜない）

### コミットメッセージの書き方

```
種別: 変更内容の概要

例：
feat: 掃除項目の追加フォームを実装
fix: 状態判定のロジックを修正
style: カードのデザインを更新
refactor: calcElapsedDays関数を整理
docs: CLAUDE.mdにGit運用ルールを追加
```

| 種別 | 使う場面 |
|------|----------|
| `feat` | 新しい機能を追加したとき |
| `fix` | バグを修正したとき |
| `style` | デザイン・見た目の変更 |
| `refactor` | 動きは変えずにコードを整理したとき |
| `docs` | ドキュメントの変更 |

### 作業の流れ

```bash
# 変更したファイルを確認
git status

# 変更内容を確認
git diff

# ステージングに追加（ファイル名を指定する）
git add index.html style.css app.js

# コミット
git commit -m "feat: ○○機能を追加"

# GitHubにプッシュ
git push origin main
```
